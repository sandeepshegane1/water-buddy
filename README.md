# Water Buddy Android
Open this folder in Android Studio, wait for the project sync, then use **Build > Build APK(s)**. The debug APK will be at `app/build/outputs/apk/debug/app-debug.apk`.

The app uses Android's installed text-to-speech voice, with a higher pitch to sound cute. Voice can be turned on/off from the app and from the reminder notification.
